package Trees_CheckAgain;

/*Given a Binary Tree where each node has positive and negative values.
 *  Convert this to a tree where each node contains the sum of the left and 
 *  right sub trees in the original tree. The values of leaf nodes are changed to 0.

For example, the following tree 

                  10
               /      \
	     -2        6
           /   \      /  \ 
	 8     -4    7    5
should be changed to

                 20(4-2+12+6)
               /      \
	   4(8-4)      12(7+5)
           /   \      /  \ 
	 0      0    0    0
*/


// by using stack.  push all the nodes 1 by 1 to it.  and pop from last 
//while(stack.isEmpty())
//{
//  popNode=stack.opo()
// if(popoNode.getLeft!-null) and popNode.getright!==null)
//{
	//int k1=popNode.getLeft()+popNode.getright();
//popNode.setKey(k1);
//}
//}
public class ConvertTreeToItsSumTree {

}
